package gr.aueb.cf.carrentalapp.core.enums;

/**
 * Enum representing the gender of a user.
 * This enum is used to categorize users based on their gender identity.
 */
public enum Gender {
    MALE,
    FEMALE,
    OTHER
}
